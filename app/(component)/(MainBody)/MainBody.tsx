import Lunch from "../(Lunch)/Lunch";
import UpperBody from "../(UpperBody)/UpperBody";

const MainBody = () => {
  return (
    <div>
      <UpperBody />
    </div>
  );
};

export default MainBody;
